apiVersion: apps/v1
kind: Deployment

metadata:
  name: {{ include "storage-lab.fullname" . }}

  labels:
    {{- include "storage-lab.labels" . | nindent 4 }}
    app: {{ .Values.labels.app }}
    tier: {{ .Values.labels.tier }}

spec:
  replicas: {{ .Values.replicaCount }}

  revisionHistoryLimit: 5

  strategy:
    type: RollingUpdate

    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1

  selector:
    matchLabels:
      app: {{ .Values.labels.app }}
      tier: {{ .Values.labels.tier }}

  template:
    metadata:
      labels:
        app: {{ .Values.labels.app }}
        tier: {{ .Values.labels.tier }}
        environment: {{ .Values.labels.environment }}
        version: {{ .Values.labels.version }}

    spec:
      containers:
        - name: nginx-container

          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}

          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name

            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace

          command:
            - /bin/sh
            - -c

          args:
            - |
              mkdir -p /usr/share/nginx/html

              cat <<EOF > /usr/share/nginx/html/index.html
              <html>
                <body>
                  <h1>Helm Kubernetes Storage Lab</h1>
                  <p>Pod: ${POD_NAME}</p>
                  <p>Namespace: ${POD_NAMESPACE}</p>
                  <p>Managed by: Helm</p>
                  <p>Chart version: {{ .Chart.Version }}</p>
                  <p>Application version: {{ .Chart.AppVersion }}</p>
                </body>
              </html>
              EOF

              while true; do
                echo "$(date) - ${POD_NAME} wrote data through Helm deployment" \
                >> /mnt/shared-storage/${POD_NAME}.txt
                sleep 15
              done &

              nginx -g "daemon off;"

          resources:
            {{- toYaml .Values.resources | nindent 12 }}

          volumeMounts:
            - name: shared-storage
              mountPath: /mnt/shared-storage

          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.service.targetPort }}

            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}

          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.service.targetPort }}

            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}

      volumes:
        - name: shared-storage

          persistentVolumeClaim:
            claimName: {{ default (printf "%s-pvc" (include "storage-lab.fullname" .)) .Values.persistence.existingClaim }}