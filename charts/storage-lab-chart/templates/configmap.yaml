apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "storage-lab.fullname" . }}-config

  labels:
    {{- include "storage-lab.labels" . | nindent 4 }}

data:
  application-name: storage-lab
  environment: {{ .Values.labels.environment }}